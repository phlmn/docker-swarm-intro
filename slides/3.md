# Deploy Services

```sh
scp -r ./ root@139.59.129.22: # Copy config to master
```

```sh
docker network create -d overlay public --opt encrypted
docker stack deploy --compose-file load-balancer/docker-compose.yml load-balancer
docker stack deploy --compose-file first-instance/docker-compose.yml first-instance
docker stack deploy --compose-file second-instance/docker-compose.yml second-instance
```